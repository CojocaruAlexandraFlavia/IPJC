using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CheckPoint : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
//      private void OnTriggerEnter(Collider other)
//   {
//        if(other.gameObject.tag == "Player" && gameObject.tag != "Checked"){
//         GameObject.FindWithTag("Flag").GetComponent<MeshRenderer>().material.color = Color.magenta;
//         gameObject.tag = "Checked";
//         GameObject.FindWithTag("Flag").tag = "CheckedFlag";
        
//         GameObject g = GameObject.FindWithTag("LastCheckPoint");
//         g= gameObject;
//         Debug.Log(g.transform.position);
//     }

               
  //}
   
}
